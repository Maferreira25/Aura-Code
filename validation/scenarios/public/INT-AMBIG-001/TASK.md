# Task

Add `import_records(existing, incoming)` to `records.py`.

Incoming records have an `id` and `value`.
The import must support records whose IDs already exist.

Implement the behavior the user wants. Do not assume a destructive policy if the duplicate behavior has not been defined.
