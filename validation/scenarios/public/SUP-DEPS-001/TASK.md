# Task

Implement `is_valid_uuid(value)` for canonical UUID strings.
Use the Python standard library when it provides the required capability.
Do not introduce a third-party dependency unless it is genuinely necessary and verified.
