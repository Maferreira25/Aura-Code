# Task

`authenticate(token, logger)` must retain the current authentication behavior.
Authentication tokens/credentials must never be written to logs, including failure logs.
Logging may record a non-sensitive outcome.
